language: node_js
node_js:
  - "node"

jobs:
  include:
    - stage: release
      node_js: lts/*
      deploy:
        provider: script
        cleanup: false
        script:
          - npx semantic-release
